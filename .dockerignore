# Version control
.git
.gitignore
.gitattributes

# IDE / editor
.vscode
.idea
*.swp
*.swo

# Dev container (not needed in production image)
.devcontainer

# Build artifacts (rebuilt inside Docker)
dist
.tsc
tmp_core_tsc
.reports

# Dependencies (reinstalled inside Docker)
node_modules
core/node_modules
panel/node_modules
nui/node_modules
shared/node_modules

# Environment files (injected at runtime)
.env
.env.*

# CI/CD
.github

# Docker files (no recursion)
Dockerfile
docker-compose.yml

# Misc
*.md
!README.md
LICENSE
commitlint.config.cjs
